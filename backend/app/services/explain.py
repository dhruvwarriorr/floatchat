"""Computation-transparency and provenance-panel service boundary.

The planned service will compose the actual QC rule, retained/excluded counts,
distinct-float count, current aggregate, baseline mean/std/n, anomaly score,
source/version, selection, and proxy caveats.  It must never substitute
illustrative frontend values for computed scientific evidence.
"""
